@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="text-center py-5">
    <i class="bi bi-shield-lock" style="font-size: 3rem; color: var(--tv-green);"></i>
    <h2 class="mt-3" style="color: var(--tv-green-dark);">Acceso denegado</h2>
    <p class="text-muted">No tienes permisos para ver esta página.</p>
    <a asp-controller="Products" asp-action="Index" class="btn btn-primary mt-2">Volver al catálogo</a>
</div>
